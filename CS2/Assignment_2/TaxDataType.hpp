#ifndef TAXDATATYPE_HPP_INCLUDED
#define TAXDATATYPE_HPP_INCLUDED

// Defining my own struct
struct taxPayer
{
    // Declaring all the variables within the struct
    float taxRate;
    float income;
    float taxes;
};

#endif // TAXDATATYPE_HPP_INCLUDED
